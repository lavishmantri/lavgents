# =============================================================================
# LLM / AI Provider
# =============================================================================
# Set to "ollama" to use local Ollama instance, or "openai" (default) for cloud
LLM_PROVIDER=openai

# OpenAI config (when LLM_PROVIDER=openai)
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o

# Ollama config (when LLM_PROVIDER=ollama)
# OLLAMA_BASE_URL=http://localhost:11434/v1
# OLLAMA_MODEL=qwen3:8b

# =============================================================================
# Speech-to-Text (Whisper)
# =============================================================================
# Local Whisper server URL (used when LLM_PROVIDER=ollama)
# WHISPER_BASE_URL=http://localhost:8080/v1

# =============================================================================
# Nango Self-Hosted Configuration
# =============================================================================
# Database password for Nango's PostgreSQL
NANGO_DB_PASSWORD=nango

# Public URL where Nango server is accessible (for OAuth callbacks)
NANGO_SERVER_URL=http://localhost:3003

# Host URL for the Nango Node SDK client
NANGO_HOST=http://localhost:3003

# Secret key for signing (CHANGE IN PRODUCTION - use: openssl rand -hex 32)
# This is also used by the Node SDK client for authentication
NANGO_SECRET_KEY=nango-secret-key-change-in-production

# Encryption key for tokens (CHANGE IN PRODUCTION - use: openssl rand -hex 32)
NANGO_ENCRYPTION_KEY=

# OAuth callback URL
NANGO_CALLBACK_URL=http://localhost:3003/oauth/callback

# Disable telemetry
NANGO_TELEMETRY=false

# =============================================================================
# Your OAuth App Credentials (register at each provider's developer console)
# These are used when setting up integrations in Nango dashboard
# =============================================================================

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=xxx
GOOGLE_CLIENT_SECRET=xxx

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID=xxx
GITHUB_CLIENT_SECRET=xxx

# GitLab OAuth (https://gitlab.com/-/profile/applications)
GITLAB_CLIENT_ID=xxx
GITLAB_CLIENT_SECRET=xxx

# Slack OAuth (https://api.slack.com/apps)
SLACK_CLIENT_ID=xxx
SLACK_CLIENT_SECRET=xxx
SLACK_SIGNING_SECRET=xxx

# Discord OAuth (https://discord.com/developers/applications)
DISCORD_CLIENT_ID=xxx
DISCORD_CLIENT_SECRET=xxx
DISCORD_BOT_TOKEN=xxx

# Notion OAuth (https://www.notion.so/my-integrations)
NOTION_CLIENT_ID=xxx
NOTION_CLIENT_SECRET=xxx
# Or use internal integration token
NOTION_API_KEY=xxx

# WhatsApp Business API (https://developers.facebook.com/)
WHATSAPP_PHONE_NUMBER_ID=xxx
WHATSAPP_ACCESS_TOKEN=xxx
WHATSAPP_VERIFY_TOKEN=xxx

# Fireflies.ai (https://app.fireflies.ai/api)
FIREFLIES_API_KEY=xxx

# Telegram Bot API (https://core.telegram.org/bots#botfather)
TELEGRAM_BOT_TOKEN=xxx

# =============================================================================
# Webhook Secrets (for verifying incoming webhooks)
# =============================================================================
GITHUB_WEBHOOK_SECRET=xxx
GITLAB_WEBHOOK_SECRET=xxx
SLACK_WEBHOOK_SECRET=xxx
DISCORD_WEBHOOK_SECRET=xxx
TELEGRAM_WEBHOOK_SECRET=xxx

# =============================================================================
# Application Configuration
# =============================================================================
# Port for your webhook server
WEBHOOK_PORT=3100

# Your application's base URL (for OAuth redirects back to your app)
APP_BASE_URL=http://localhost:3000